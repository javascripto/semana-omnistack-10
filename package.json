{
    "name": "devRadar",
    "version": "1.0.0",
    "main": "backend/src/index.js",
    "scripts": {
        "install": "cd web && npm install && npm run build && cd ../backend && npm install",
        "start": "cd backend && npm start"
    }
}
